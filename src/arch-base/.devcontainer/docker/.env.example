# Docker Compose environment variables

# Username for the container user
USERNAME=vscode

# User and group IDs (use your host user IDs)
PUID=1000
PGID=1000

# Additional Docker Compose file to load (optional)
DOCKER_COMPOSE_FILE=docker-compose.yml
